package com.charakhovich.parser.impl;

import com.charakhovich.exception.TextException;
import com.charakhovich.text.composite.TextPart;
import com.charakhovich.text.composite.TypeTextPart;
import com.charakhovich.text.composite.impl.TextComposite;
import org.testng.annotations.Test;

import java.util.List;

public class DataParserTest {

  /*  @Test
    public void testParse() throws TextException {
        TextParser textParser = TextParser.getInstance();
        TextComposite textComposite=textParser.parse("\tIt has survived - not only (five) centuries, but also the leap into " +
                "electronic typesetting, remaining  unchanged. ");
        List<TextPart> t=textComposite.getListTextPart(TypeTextPart.LEXEME);
        t.toString();
    }*/
}